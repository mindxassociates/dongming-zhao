# Therapy with Dongming

Astro static website with Pages CMS editing and Cloudflare Pages deployment.

```bash
npm install
npm run dev
npm run build
```

Production: `https://therapywithdongming.com`  
Cloudflare Pages: build command `npm run build`, output directory `dist`, production branch `main`.
